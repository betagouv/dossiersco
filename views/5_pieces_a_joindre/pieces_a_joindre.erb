<% @section = 5 %>
<%= erb :'partials/progression_primaire' %>

<div class="col-12 col-md-8">

  <h2>Pièces à joindre</h2>

  <hr>

  <span style="display:block; height: 20px;"></span>

  <form>

    <% documents_demandes = ["Photo d'identité de l'élève", "Pièce d'identité de l'élève", "Attestation d'assurance scolaire", "Notification du tarif de cantine"] %>

    <% pourquoi_on_le_demande = ["Pour faire les trombinoscopes en attendant les photos de classe", "Pour vérifier... l'identité de l'élève !", "Pour participer aux sorties et voyages scolaires", "Pour facturer équitablement la cantine"]%>

    <div class="card-deck">
      <% for i in 0..1 %>
        <% @document = documents_demandes[i] %>
        <% @explication = pourquoi_on_le_demande[i] %>
        <%= erb :'partials/carte_upload' %>
      <% end %>
    </div>

    <div class="card-deck">
      <% for i in 2..3 %>
        <% @document = documents_demandes[i] %>
        <% @explication = pourquoi_on_le_demande[i] %>
        <%= erb :'partials/carte_upload' %>
      <% end %>
    </div>

  </form>

  <%= erb :'partials/precedent_suivant', :locals => { :precedent => "droit_image", :suivant => "validation" } %>

</div><!--/span-->
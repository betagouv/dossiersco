<% @section = 4 %>
<%= erb :'partials/navigation' %>

<div class="col-12 col-md-8">

  <h2>Pièces à joindre</h2>

  <hr>

  <span style="display:block; height: 20px;"></span>

  <form>

    <% documents_demandes = ["Photo d'identité de l'élève", "Attestation d'assurance scolaire", "Extrait du jugement relatif à la garde de l'enfant"] %>

    <% pourquoi_on_le_demande = ["Pour coller une photo récente sur les carnets de correspondance", "Pour participer aux sorties et voyages scolaires, notamment", "En cas de séparation, de divorce ou de tutelle datant de moins d'un an"]%>

    <div class="card-deck">
      <% for i in 0..1 %>
        <% @document = documents_demandes[i] %>
        <% @explication = pourquoi_on_le_demande[i] %>
        <%= erb :'partials/carte_upload' %>
      <% end %>
    </div>

<div class="row">
<div class="col-6">
    <div class="card">
      <h5 class="card-header"><%= documents_demandes[2] %></h5>
      <div class="card-body">
        <p class="card-text"><%= pourquoi_on_le_demande[2] %></p>
        <div class="card-relat">
    	    <button type="button" class="btn btn-primary btn-sm"><label class="upload">Parcourir<input type="file" hidden></label></button>
    	    <button type="button" class="btn btn-secondary btn-sm" disabled>Envoyer</button>
        </div>
      </div>
    </div>
</div>
</div>

  </form>

  <%= erb :'partials/precedent_suivant', :locals => { :precedent => "r3", :suivant => "r5" } %>

</div><!--/span-->
<section class="section">
  <div class="container">
    <h1 class="separation">Collège <%= @dossier_eleve.etablissement.nom %></h1>

    <p>Pour réinscrire votre enfant
    <strong>
      <%= @dossier_eleve.eleve.prenom %> <%= @dossier_eleve.eleve.nom %>
    </strong>
    au collège
    <strong id="etablissement">
      <%= @dossier_eleve.etablissement.nom %>.
    </strong>
    </p>

    <p>Veuillez vous munir des pièces administratives suivantes :</p>
    <ul>
      <% @dossier_eleve.etablissement.pieces_attendues.each do |piece| %>
        <li><strong><%= piece.nom %></strong></li>
        <% if piece.explication.present? %>
          <p><%= markdown(piece.explication) %></p>
        <% end %>
      <% end %>
    </ul>
    <p>et mettre à jour vos coordonnées sur ce site avant le <strong id="date-limite"><%= @dossier_eleve.etablissement.date_limite %></strong>.</p>

    <% if @dossier_eleve.etablissement.mot_accueil.present? %>
      <section><%= markdown(@dossier_eleve.etablissement.mot_accueil) %></section>
    <% end %>

    <div class="text-center">
      <form action="/accueil"  method="post">

        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <button type="submit" class="button boutton-enregistrer mt-2">Commencer l’inscription</button>
      </form>

    </div>
  </div>
</section>

<section class="section">
  <h1 class="separation">Tableau de bord</h1>

  <div>
    Collège <%= @agent_connecte.etablissement.nom %> : <span id="total_dossiers" class="badge badge-danger"><%= total_dossiers %></span> dossiers.
  </div>

  <div>Nombre de notes : <%= notes.reject { |a| a == 0 }.count  %></div>

  <div>Moyenne : <%= moyenne %></div>
  <br />
  <div>
    <ul>
    <% dossiers_avec_commentaires.each do |dossier| %>
      <li>
        <a href="/agent/eleve/<%= dossier.identifiant %>" ><%= l(dossier.date_validation_famille, format: :short) %> : <strong><%= dossier.satisfaction %></strong> : </a>
        <%= dossier.commentaire %>
      </li>
    <% end %>
    </ul>
  </div>
</section>
